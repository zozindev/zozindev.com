---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
  description: string;
  date: string;
  category: string;
  canonicalPath: string;
  includeAdsenseScript?: boolean;
}

const { title, description, date, category, canonicalPath, includeAdsenseScript = true } = Astro.props;
---

<BaseLayout
  title={`${title} - zozindev`}
  description={description}
  canonicalPath={canonicalPath}
  ogType="article"
  ogTitle={title}
  ogDescription={description}
  shellClass="site-shell post"
  includeAdsenseScript={includeAdsenseScript}
>
  <a class="text-link" href="/blog/">Blog</a>
  <article>
    <header>
      <p class="eyebrow">{category}</p>
      <h1>{title}</h1>
      <p class="post-meta">{date}</p>
    </header>
    <slot />
  </article>
  <footer class="site-footer">
    <a href="/">Home</a>
    <a href="/blog/">Blog</a>
    <a href="/projects/">Projects</a>
    <a href="/about/">About</a>
    <a href="/contact/">Contact</a>
  </footer>
</BaseLayout>
